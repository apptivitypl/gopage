# C005 — unterminated directive

## Message

```
error[RILL-C005]: the outlet directive is never closed
```

## Why this is rejected

`{%` opens a directive. Without `%}` the parser cannot tell where the directive ends and the markup resumes.

## How to fix it

Close it with `%}`.
