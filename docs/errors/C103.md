# C103 — outlet outside a layout

## Message

```
error[RILL-C103]: outlet is only meaningful inside a layout
```

## Why this is rejected

`{% outlet %}` marks where a child renders. A page has no child, so the directive would always render nothing.

## How to fix it

Remove the directive from the page. If you wanted to wrap other routes, move the markup into `layout.rill`.
