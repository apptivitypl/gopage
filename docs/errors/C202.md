# C202 — unclosed group

## Message

```
error[RILL-C202]: expected ) to close the group, found }}
```

## Why this is rejected

A group or an index that is never closed makes the rest of the expression ambiguous.

## How to fix it

Close the group with `)` or the index with `]`.
