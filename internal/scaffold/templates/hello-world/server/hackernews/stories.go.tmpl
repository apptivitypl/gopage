package hackernews

type Story struct {
	Title string `json:"title"`
	URL   string `json:"url"`
	By    string `json:"by"`
	Score int    `json:"score"`
}

var offline = []Story{
	{Title: "the upstream was not reachable, so this list is the built-in one", URL: "https://github.com/sonquer/rill", By: "rill", Score: 1},
	{Title: "the same code answers on a binary and on a worker", URL: "https://github.com/sonquer/rill", By: "rill", Score: 2},
	{Title: "one request, one json body, nothing held open", URL: "https://github.com/sonquer/rill", By: "rill", Score: 3},
}
